# Recruitment System

A web-based recruitment system and talent network centered in Enugu and South East Nigeria.

## Run Locally

**Prerequisites:**  Node.js


1. Install git add .
git commit -m "Configure GitHub Pages deployment"
git push origin maindependencies:
   `npm install`
2. Run the app:
   `npm run dev`
